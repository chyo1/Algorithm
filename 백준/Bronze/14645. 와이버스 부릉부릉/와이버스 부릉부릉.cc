#include <stdio.h>
int main() {
	int N, K, A;
	scanf("%d %d", &N, &K);
	for (int i = 0; i < N; i++) {
		for (int j = 0; j < K; j++) {
			scanf("%d", &A);
		}
	}
	printf("비와이");
	return 0;
}